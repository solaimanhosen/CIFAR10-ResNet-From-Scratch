# CIFAR10-ResNet-From-Scratch
Implementation and evaluation of deep residual networks for CIFAR-10 image classification using PyTorch, including original ResNet blocks, full pre-activation bottleneck blocks, batch normalization, dropout, and hyperparameter tuning.
